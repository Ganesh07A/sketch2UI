User selects sketch image
        ↓
Next.js frontend packs it as form data
        ↓
Sends request to FastAPI endpoint
        ↓
FastAPI reads the file
        ↓
Sends back confirmation JSON
        ↓
Frontend shows response text
